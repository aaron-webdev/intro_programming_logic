# The input function
- input([prompt])
    - first_name = input("Enter your first name: ")
    - print(f"Hello, {first_name}!")
- ALL INPUTS WILL BE ASSIGNED AS A STRING
    - int(data) # converts data to integer
    - float(data) # converts data to float
    - round(number [,digits])  # rounds data to {digits} past 0

- quantity = input("Enter the quantity: ")  # gets string
- quantity = int(quantity)  # converts to integer
    - quantity = int(input("ENter the quantity: "))

