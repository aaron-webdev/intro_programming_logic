# Functions and Modules
## 

- def functionName([arguments]):
    - statements
    - set a default value by setting a statement in the declaration statment

- Call main within an If statement:  ONLY INCLUDE AT THE BOTTOM OF THE MAIN PROGRAM FILE
    - if __name__ == "__main__":
        - main()

- import fileName as ProgName
    - import temperature.py as temp
    - then use "temp" to reference the file

 - random()
    -ranNum = random.random() gives a number 0-1
    